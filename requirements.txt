# FastAPI framework - modern, fast web framework
# Note: FastAPI itself requires pydantic, but we don't use it in our code
fastapi>=0.104.0

# ASGI server for production
uvicorn[standard]>=0.24.0

# HTTP client for Teams webhook calls
httpx>=0.25.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
